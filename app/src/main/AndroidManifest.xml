<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhaagenda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinhaAgenda">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".domlayout" />
        <activity android:name=".seglayout" />
        <activity android:name=".terlayout" />
        <activity android:name=".quarlayout" />
        <activity android:name=".quilayout" />
        <activity android:name=".sexlayout" />
        <activity android:name=".sablayout" />
    </application>

</manifest>